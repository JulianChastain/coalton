#!/bin/bash
# Coalton REPL with type display
# Usage: ./scripts/coalton-repl

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"
cd "$PROJECT_DIR"
exec sbcl --noinform --load "$SCRIPT_DIR/coalton-repl.lisp"
